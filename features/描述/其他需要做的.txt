需要做的。

1、现在所有的办理，都是 在所有任务 列表，获取 第一条流程的办理按钮。
没有进行  任务名称，任务信息  去匹配对应的 办理按钮。

2、没有 定义 异常判断 没有验证是否可能会出错，以及  wait （等待某个元素加载完成）  如果遇到 页面报错 500 , 没有定义异常，会直接导致测试失败。


3、没有 退改流程 。 不知道怎样用  cucumber 来定义  驳回，退改   的流程。


4、 在 页面上，没有 对 菜单 进行验证是否打开。  如果已经打开菜单，再 重复打开，会导致子菜单不可见，因此 子菜单 的点击事件就会报错。


5、


下面是可能发生问题 的原因，和可能发生的一些问题。

this.drive.sleep(  1000) ;   单位是毫秒，1000  毫秒  是1 秒。




一个任务流程中，所需要 录入脚本的， 是  所有需要  手动操作  的步骤，  自动任务和，流程图中，所有的  ？  任务，都不需要。

但是，所有的自动任务，和 ？ 任务，都必须知道，当前任务的分支条件， 因为不同的条件，决定不同的流程，并且把所有 影响流程的任务条件，配置到      当前任务- base.page.js     中，以方便测试。